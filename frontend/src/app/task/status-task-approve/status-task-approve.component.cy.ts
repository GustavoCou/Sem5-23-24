import { StatusTaskApproveComponent } from './status-task-approve.component'

describe('StatusTaskApproveComponent', () => {
  /*it('should mount', () => {
    cy.mount(StatusTaskApproveComponent)
  })*/
})
