import { ConfirmCancelUserComponent } from './confirm-cancel-user.component'

describe('ConfirmCancelUserComponent', () => {
  it('should mount', () => {
    cy.mount(ConfirmCancelUserComponent)
  })
})